<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="mm.myappwidget">

    <application android:allowBackup="true" android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher" android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <receiver android:name=".WidgetActivity" android:label="MyAppWidget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider"
                android:resource="@xml/myappwidget" />
        </receiver>
        <service android:name="MyService">
            <intent-filter>
                <action android:name="jp.tuyano.MyService.ACTION_BTNCLICK" />
            </intent-filter>
        </service>
    </application>

</manifest>
